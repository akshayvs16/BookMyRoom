<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/urbanist_bold</item>

    </style>
    <style name="Base.Widget.AppCompat.CompoundButton.CheckBox" parent="Theme.AppCompat.DayNight">
        <item name="android:background">@color/primary500</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize other attributes -->
        <item name="android:windowBackground">@color/white</item>
    </style>

</resources>